import 'package:flutter/material.dart';

class tobankaccscreen extends StatelessWidget {
  const tobankaccscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
