import 'package:flutter/material.dart';

class tomobilescreen extends StatelessWidget {
  const tomobilescreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
